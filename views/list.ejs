<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Day of the Week</title>
  </head>
  <body>
    <% if (dayOfWeek === "Saturday" || dayOfWeek === "Sunday") { %>
    <h1 style="color: purple">Today is <%= dayOfWeek %>!</h1>
    <% } else { %>
    <h1 style="color: blue">Today is <%= dayOfWeek %>!</h1>
    <% } %>
  </body>
</html>
